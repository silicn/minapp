<!--pages/home/home.wxml-->
<view class="page">
    <view class="header">
        <mp-cells>
            <mp-cell hover wx:for="{{dataSource}}" wx:key="{{item}}" style="font-size:14px">
                <cell-view name = "{{item.title}}" time="{{item.created_name}}" content="{{item.content}}"></cell-view>
            </mp-cell>
        </mp-cells>
 
    </view>
    <view class="body"></view>
    
</view>
